Screen {
    layout: vertical;
    padding: 1 2;
}

#menu-root {
    width: 100%;
}

#status {
    margin-bottom: 1;
}

.menu-title {
    text-style: bold;
    color: cyan;
    margin: 1 0;
}

#menu {
    border: round cyan;
    padding: 1;
    height: auto;
}
